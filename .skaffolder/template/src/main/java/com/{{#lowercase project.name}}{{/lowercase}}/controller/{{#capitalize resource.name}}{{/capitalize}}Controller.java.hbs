**** PROPERTIES SKAFFOLDER ****
{
    "forEachObj": "resource",
    "_partials": []
}
**** END PROPERTIES SKAFFOLDER ****
package com.{{#lowercase project.name}}{{/lowercase}}.controller;

import org.springframework.web.bind.annotation.RestController;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.context.annotation.PropertySource;

import com.{{#lowercase project.name}}{{/lowercase}}.controller.base.{{capitalize resource.name}}BaseController;

@RestController
@PropertySource("classpath:${configfile.path}/{{project.name}}.properties")
@RequestMapping(value="${endpoint.api}", headers = "Accept=application/json")
public class {{capitalize resource.name}}Controller extends {{capitalize resource.name}}BaseController {

	//OVERRIDE HERE YOUR CUSTOM CONTROLLER

}
